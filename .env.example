FASTMAIL_API_TOKEN=your-fastmail-app-password
MCP_AUTH_TOKEN=your-bearer-token-for-mcp-endpoint

# Host directory to bind-mount as the in-container download dir
# (/data/downloads). Defaults to ./downloads next to docker-compose.yml.
# FASTMAIL_DOWNLOAD_DIR_HOST=/srv/docker/fastmail-mcp/downloads
